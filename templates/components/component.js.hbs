const ApplicationComponent = require('./{{topDir name}}application_component');

/**
 * {{classFullName name}}Component
 */
module.exports = class {{className name}}Component extends ApplicationComponent {
  static definer() {
    name('{{fileName name}}');
  }
}
