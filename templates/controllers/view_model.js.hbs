const ApplicationViewModel = require('./{{topDir name}}../application_view_model');

/**
 * ViewModel of {{classFullName name}}Controller
 */
module.exports = class {{className name}} extends ApplicationViewModel {
  /** ViewModel definition **/
  static definer() {
    name('{{fileName name}}');
  }
};
