const ApplicationController = require('./{{topDir name}}application_controller');

/**
 * {{classFullName name}}Controller
 */
module.exports = class {{className name}}Controller extends ApplicationController {
  static definer() {
    name('{{fileName name}}');
  }
}
